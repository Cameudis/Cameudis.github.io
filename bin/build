#!/usr/bin/env sh

set -eu

if [ ! -x node_modules/.bin/pagefind ]; then
  echo "Pagefind is not installed. Run 'npm ci' first." >&2
  exit 1
fi

bin/normalize-link-spacing
bundle exec jekyll build
bin/cache-external-link-icons
npm run search:index
